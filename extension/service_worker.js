/**
 * Loadable service worker stub for Chrome (pre-build).
 * Source of truth: service_worker.ts. Build emits dist/service_worker.js.
 *
 * This file is not the production entry — load the extension from dist/
 * after `npm run build`.
 */
throw new Error(
  'extension/service_worker.js is a source stub — load the extension from dist/ after npm run build',
);
